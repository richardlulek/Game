node_modules
dist
dist-ssr
*.local
.DS_Store
*.log
# Ignore local Claude Code state, but keep the shared SessionStart hook
# and project settings tracked so every web session gets the same setup.
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/settings.local.json

# Scratchpad for throwaway working files (logo explorations, notes)
scratchpad/
