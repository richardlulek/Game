/* Delad ruta som PerfProbe (inuti Canvas) skriver och FpsMeter (DOM) läser –
   förra bildrutans draw calls och trianglar ur three's renderer.info. */
export const perfStats = { calls: 0, tris: 0 };
